# README demo

Runs several SDK flows used in the main README. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/Readme/Readme.csproj
```

This example makes live API calls and may create billable resources.
