# Files and snapshots

Exercises file operations and sandbox snapshots. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/FilesAndSnapshots/FilesAndSnapshots.csproj
```

This example makes live API calls and may create billable resources.
