# Private overlay network

Connects a sandbox to a private network and verifies it. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/Network/Network.csproj
```

This example makes live API calls and may create billable resources.
