# Desktop and noVNC

Exercises desktop automation and a noVNC connection. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/Desktop/Desktop.csproj
```

This example makes live API calls and may create billable resources.
