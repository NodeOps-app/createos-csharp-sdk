# Managed process lifecycle

Exercises managed process input, output, and termination. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/ManagedProcess/ManagedProcess.csproj
```

This example makes live API calls and may create billable resources.
