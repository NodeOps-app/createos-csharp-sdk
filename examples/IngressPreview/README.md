# Ingress preview

Serves a page in a sandbox through a preview URL. See [Program.cs](Program.cs) for the code.

From the repository root:

```sh
export CREATEOS_API_KEY="your-api-key"
dotnet run --project examples/IngressPreview/IngressPreview.csproj
```

This example makes live API calls and may create billable resources.
